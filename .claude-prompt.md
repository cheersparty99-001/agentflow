You need to simplify AgentFlow by removing all insurance-related code and updating the UI.

Read /root/agentflow/CLAUDE.md first for the full plan.

Then do these steps in order:

## Step 1: Delete insurance files
Delete these files:
- /root/agentflow/routers/policies.py
- /root/agentflow/routers/agents.py
- /root/agentflow/templates/policies.html
- /root/agentflow/templates/agents.html
- /root/agentflow/services/renewal_reminder.py
- /root/agentflow/services/enquiry_handler.py
- /root/agentflow/services/stub_modules.py
- /root/agentflow/services/sheets.py

## Step 2: Update main.py
Read the existing file first. Then:
- Remove 'policies' and 'agents' from the import line
- Remove app.include_router(policies.router) and app.include_router(agents.router)
- Remove all insurance demo data: demo_logs, demo_policies, rich_logs, the entire block from 'from datetime import datetime, timedelta' to 'app.state.demo_logs.extend(rich_logs)'
- Remove the Telegram webhook setup block (from 'if cfg.TELEGRAM_BOT_TOKEN' to end)
- Keep only Sales Automation init and missing env vars check

## Step 3: Update base.html
Read existing file first. Then:
- Change subtitle from 'Insurance Agent Platform' to 'B2B Sales Automation Platform'
- The first Dashboard link should point to /sales/dashboard
- Remove Policies link, Agents link
- Remove the 'Sales Automation' subheading - promote Sales Dashboard, Leads, Pipeline to top level
- Do the same in the mobile menu

The sidebar should have: Dashboard (-> /sales/dashboard), Leads (-> /sales/leads), Pipeline (-> /sales/pipeline), Settings (-> /settings), Admin (if admin)

## Step 4: Replace dashboard.py router
Read /root/agentflow/routers/dashboard.py first. Replace with:

from fastapi import APIRouter, Request
from fastapi.responses import RedirectResponse

router = APIRouter()

@router.get('/dashboard')
async def dashboard(request: Request):
    return RedirectResponse(url='/sales/dashboard')

## Step 5: Keep auth working
Do NOT change:
- /root/agentflow/routers/auth.py
- /root/agentflow/routers/admin.py  
- /root/agentflow/templates/login.html

After each step, verify: cd /root/agentflow && python -c 'import main; print("OK")'
