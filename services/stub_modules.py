"""Stub modules for unimplemented agents."""
def enquiry_handler(account_id, config=None):
    print("Enquiry Handler module — already implemented")
def claims_tracker(account_id, config=None):
    print("Claims Tracker module — not yet implemented"); return None
def annual_report(account_id, config=None):
    print("Annual Report module — not yet implemented"); return None